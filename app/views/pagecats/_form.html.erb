<%= form_for(@pagecat) do |f| %>
  <% if @pagecat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pagecat.errors.count, "error") %> prohibited this pagecat from being saved:</h2>

      <ul>
      <% @pagecat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
